# formulaire-osteopathie-animale
Formulaire de brief client NovaSpot.

`index.html` est **généré** : ne pas le modifier ici. Le modèle commun aux six formulaires et les données de chaque métier sont dans `~/.claude/skills/novaspot/formulaires/` (`modele.html`, `secteurs.json`) ; corriger là, puis lancer `python3 generer.py`.
